# Upstream skill — has its own git repo, tracked separately
base/

# Campaign data — kept local only, user backs up separately
campaigns/

# Per-machine state
.claude/settings.local.json

# Python
console/.venv/
**/__pycache__/
*.pyc
*.pyo

# OS / editor noise
.DS_Store
Thumbs.db
.vscode/
.idea/
